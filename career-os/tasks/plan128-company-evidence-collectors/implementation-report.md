# plan128 구현과 검증 결과

네 단계를 순서대로 구현하고 각 단계의 검증을 통과한 뒤 다음 단계로 넘어갔다.
외부 배포와 운영 데이터 변경, 푸시와 PR 생성은 하지 않았다.

| 단계 | 구현 결과 | 커밋 | 검증 결과 |
| --- | --- | --- | --- |
| 01 | 회사 판정의 근거가 없을 때 세 축을 `unknown`으로 저장하고 추천 tier와 신뢰도를 비울 수 있게 했다. 기존 평가와 DB 열의 호환성을 확인했다 | `c8761cd` | API 테스트 135개 통과, API와 루트 타입 검사 통과, MySQL 세 열의 NULL 허용 확인 |
| 02 | 기술 블로그 RSS, GitHub organization, 활성 공고 수집기와 회사별 수집 주소 및 활성 공고 조회 API를 추가했다 | `7dace71` | 스크립트 테스트 129개, API 테스트 137개, 두 타입 검사와 한국어 검사 통과 |
| 03 | OpenDART 직원·재무 정보와 Blind 회사 평점 수집기를 추가하고 현재 직장을 `benchmark`로 판정 큐에 넣었다. 저장된 근거 ID를 모델 입력에 전달했다 | `ca491c8` | 스크립트 테스트 137개, API 테스트 139개, 두 타입 검사와 한국어 검사 통과. 수집기 테스트에서 외부 네트워크를 호출하지 않았고 인증키 형태 문자열 검사 결과 0건 |
| 04 | 축별 판정 기준을 적고 추천 API와 HTML에 세 축, 축별 근거 링크, 현재 직장 비교 기준을 연결했다. 내부 tier 표시를 없애고 비공개 `assessment`를 추천 응답에서 제외했다 | 이 문서가 포함된 커밋 | 스크립트 테스트 140개, API 테스트 140개, 두 타입 검사, 한국어 검사와 skill 검증 통과 |

마지막 단계에서는 고정 입력으로 HTML을 생성했다.
「근거 없음」은 35회, `Tier ` 표시는 0회였고 HTML 생성 명령이 종료 코드 0으로 끝났다.
두 테스트 모음 모두 실패와 건너뜀 없이 끝났다.

운영 배포는 별도 작업이다.
운영 DB migration 적용, 인증키 파일 연결, 현재 직장의 `benchmark` 설정과 회사별 수집 주소 등록은 이번 작업에서 실행하지 않았다.
